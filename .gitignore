# BMad Method / Claude Code tooling - internal planning process, not part of the product
_bmad/
.claude/

# Raw planning-process logs - not meant for public reading
**/.memlog.md
_bmad-output/brainstorming/*/brainstorm.html

# OS/editor cruft
.DS_Store
Thumbs.db

# Python (adjust once the stack is finalized)
__pycache__/
*.pyc
.venv/
venv/
.env

# PyInstaller build output - published via GitHub Releases, never committed
build/
dist/
*.spec

# Runtime state (local SQLite DB, quarantine images, settings) - never committed
data/*
!data/.gitkeep
